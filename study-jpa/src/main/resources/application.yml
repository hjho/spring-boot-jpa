spring:
  application:
    name: study-jpa

  # spring.datasource.hikari
  datasource:
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost/jpabegin?characterEncoding=utf8
      username: hjho
      password: hjho1234
      pool-name: mysql-pool
      maximum-pool-size: 10
      minimum-idle: 10
      connection-timeout: 1000  #1s
      
  # spring.jpa
  jpa:
    open-in-view: false
    
    database: mysql
    generate-ddl: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    
#    properties:
#      hibernate:
#        format_sql: true       # log 정렬
#        highlight_sql: true    # log 색 표시
#        use_sql_comments: true # log 주석 출력
#        # 자바 객체의 필드명(CamelCase)을 데이터베이스의 테이블/컬럼명(Snake_Case 등)으로 매핑하는 실제 물리적인 네이밍 규칙을 정의하는 설정.
#        physical_naming_strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
        
        
        
